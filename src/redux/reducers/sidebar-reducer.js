let initialState = {
    friendsData: [
        {id: 1, friend: 'Mike'},
        {id: 2, friend: 'John'},
        {id: 3, friend: 'Nik'}
    ]
}

const SidebarReducer = (state = initialState, action) => {

    return state;
} 

export default SidebarReducer;